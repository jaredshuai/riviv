<!-- lazypack:start block=agent-roles src=DECISIONS.md@0.3.0 gen=bfce1f5fd317b6a0 input=44cd4e152f99d359 fp=6b5211ee0017dcdd -->
# 角色与 Skill 映射表 (docs/agents/roles.md)

> 派生自 lazypack-discipline 固定层 DECISIONS.md@0.3.0（依据 lazypack-setup 内置快照编译，来源内容标识: 1c78a216d27b82666c9724701f858f2bafe2b10d；离线事实源查阅 lazypack-setup/references/DECISIONS.md）§3。
> 角色是跨项目不变的固定层；角色到具体 Skill 的映射是可换的适配层。换 Skill 只改本表。

## 1. 角色职责与防撞车边界

防撞车边界法则：同一时刻两个角色不得写入同一类文件。

| 角色 | 输入 | 输出 | 允许写（防撞车边界） |
|---|---|---|---|
| **规划者** (Planner) | 用户意图 + 调查者报告 | 规格、票据、ADR / CONTEXT 更新 | 文档、Issue；**严禁碰业务代码** |
| **调查者** (Investigator) | 规划者提出的具体问题 | 事实简报（附出处引用） | **只读**；严禁写入任何文件 |
| **执行者** (Implementer) | 单张已明确的实现票 | 代码提交（含 diff 涉及的文档行） | 代码、测试、与本 diff 直接相关的文档行 |
| **审查者** (Reviewer) | 代码 diff + 票据 + 编码标准 | 审查报告（通过 / 退回修改清单） | **只写审查意见**；严禁擅改代码 |
| **书记员** (Scribe) | 阶段性提交历史 + 全部文档 | 文档修补提交 | `docs/`、`CONTEXT.md`、`CHANGELOG.md`；**不碰代码** |
| **清道夫** (Sweeper) | 已关闭票据、已合并分支、登记册 | 清理无效临时目录、归档旧产物 | **只删和移动**已过时产物，不新增功能 |

## 2. 角色到 Skill 映射

> 状态说明规则：依据只读探测到的实际证据填写，严格区分四种正交维度：技能已安装证据、实际可调用证据、门禁接线状态与图谱建立状态，不彼此替代。种子模板中未经当前环境核验的技能状态默认标记为 [未验证]，严禁在未获实测调用证据前断言工具可用或全部就绪。

| 角色 | 当前分配的 Skill / 工具 | 状态说明（种子默认，装配时按实测证据替换） |
|---|---|---|
| **规划者** | `grill-with-docs` → `to-spec` → `to-tickets` | 技能已安装(会话技能清单在列)[已安装];实际调用本项目未验证 [未验证];门禁接线=cargo fmt/clippy/test 已接 `.githooks` 且本会话提交多次实跑 [已接线+运行已验证] |
| **调查者** | `/understand-codebase`、`/research`、codegraph/codebase-memory MCP | 技能已安装;MCP 工具已装且 `.codegraph/` 项目索引存在 [已安装+图谱已建];实际调用本项目未验证 [未验证] |
| **执行者** | `implement`(配合 `tdd`) | 技能已安装 [已安装];实际调用未验证;提交前门禁已接线并运行验证 [已接线+运行已验证] |
| **审查者** | `code-review` + 三视角对抗子代理范式 | 技能已安装 [已安装];本项目实际用同构子代理预审范式验证过多轮,skill 本体未调 [未验证] |
| **书记员** | `retro` 文档部分 | 技能已安装 [已安装];实际调用未验证 [未验证] |
| **清道夫** | 暂无专用 skill,依据 DECISIONS §4.1 执行 | 暂无专用 skill,规则/人工执行 (发版前事件驱动调用) [人工执行] |

## 3. 规划者访谈与碎片想法留存指引

> 本节由 /lazypack-setup 依据项目探测事实或用户明确选择编译生成，规范规划者在启动、推进及结束需求研讨（如 `grill-with-docs`）时的材料收敛行为。

- **当前项目留存模式**：`ideas-pool`（保留整理后的碎片想法至想法池）
- **目标路径**：`docs/ideas/inbox.md`（状态为 `exploration`）
- **触发时机与四步收敛法**：
  1. **启动访谈**：规划者在启动 `grill-with-docs` 前，确认想法池路径有效；若文件不存在则由 setup 首次生成未托管种子；
  2. **研讨推进**：成熟术语入 `CONTEXT.md`，重大决策入 `docs/adr/`；若无新术语或重大取舍则跳过，不强制制造文档；
  3. **结束收敛（先审查再落盘）**：
     - **Step 1 写前审查**：核实未决想法内容，确认来源授权与目标可见性；剔除涉及私密密钥或未脱敏文本；
     - **Step 2 决议核验**：若有新概念术语或 ADR 级决策，核实已落盘；无则跳过；
     - **Step 3 想法归集**：将未立项但有价值的构想整理追加至目标想法池 `docs/ideas/inbox.md`，标注提出日期与背景；
     - **Step 4 闭环确认**：检查文件写入状态，未采纳想法保留原始缘由。
- **防污染与授权语义硬契约**：
  - **低承诺探索性**：想法池条目默认为 `exploration`，未获授权前 Agent 严禁自发将其当作已批准的工作任务；
  - **用户显式授权即生效**：若用户在会话中明确指示“实现某想法”，即构成充分执行授权，执行者可直接编码实现，无需强制补写长规格或 ADR；但不得借采纳想法越权扩大到外部发布；
  - **流转跟踪**：想法被采纳或放弃时，注明流转去向（如关联至 Issue #N）或否决原因，保留历史考量。

## 4. 项目层实施票收尾指引

> 本指引规范执行者交付实施票与审查者核验时的收尾格式。沿用项目既有任务跟踪系统（如 Issue 评论或交付摘要），不另建台账，已有摘要直接补齐。发布、关票与提交依既有授权执行，本指引不授予额外权限。

### 4.1 收尾记录格式

- **对应变更**：已提交写提交哈希或引用；未提交注明 `未提交` 并给出本地 diff 或交付产物位置（严禁为填格式擅自提交）。
- **逐项验收表**（按原票验收条目逐项对应，占位符按实填写）：

| 验收项 | 状态 | 验证范围与证据位置 | 未验证/不适用原因、下一动作与责任人 |
|---|---|---|---|
| `<条目 1>` | `已验证通过` \| `已验证失败` \| `未验证` \| `不适用` | `<说明范围：自动化/假对象或真机/人工实测；附日志、断言或路径>` | `<未通过/未验证注明原因、后续动作与责任人（未指定写待指派）；通过留空>` |

### 4.2 验收与纪律契约

1. **证据范围**：说明验证范围，区分自动化/假对象测试与真实环境/人工实测。门禁通过不替代业务验收，票据处于 CLOSED 亦不替代验收证据；仅针对票据适用项核验，不强求无关环境测试。
2. **诚实待验**：必需项失败或未验证时结论标明待验收，严禁宣称全部完成。需求调整须引用户明确裁定，严禁自行转为不适用；未指定责任人写“待指派”，不虚构承诺。
3. **父子票结构**：父规格收尾增加子票结果、跨模块验收与遗留事项汇总。子票关闭不自动关停父票，亦不自动重开旧票。
<!-- lazypack:end block=agent-roles -->
